using System;
using System.Collections.Generic;
using System.Linq;

namespace LibrarySystem
{
    // This file is now empty because classes have been moved to their own files
    // to avoid duplication and maintain a clean project structure.
    // Classes moved: Book, Textbook, FictionBook, Reader, Reservation, Publication, BookPublication, PublishingHouse
}
